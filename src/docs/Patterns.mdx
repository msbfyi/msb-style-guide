import { Meta } from "@storybook/blocks";

<Meta title="Docs/Ornament &amp; Pattern" />

# Ornament & Pattern

Every pattern here is a closed, repeating geometric shape with a clear
end point — the rule from The Contrast Principle doc. Scale matters as
much as shape: a check this size is crisp; doubled, it loses the edge.

<div
  style={{
    display: "grid",
    gridTemplateColumns: "repeat(auto-fit, minmax(200px, 1fr))",
    gap: "16px",
    margin: "24px 0",
  }}
>
  {[
    {
      name: "Gingham — Navy",
      meta: "Dramatic + optic white · 38px",
      note: "The personal signature pattern.",
      style: {
        backgroundColor: "#F5F5F0",
        backgroundImage:
          "linear-gradient(rgba(26,58,143,.55) 50%,transparent 50%),linear-gradient(90deg,rgba(26,58,143,.55) 50%,transparent 50%)",
        backgroundSize: "38px 38px",
      },
    },
    {
      name: "Gingham — Red",
      meta: "Romantic + optic white · 38px",
      note: "Same signature, vitality colour.",
      style: {
        backgroundColor: "#F5F5F0",
        backgroundImage:
          "linear-gradient(rgba(183,28,42,.5) 50%,transparent 50%),linear-gradient(90deg,rgba(183,28,42,.5) 50%,transparent 50%)",
        backgroundSize: "38px 38px",
      },
    },
    {
      name: "Rep Stripe",
      meta: "Energy + optic white · 52px repeat",
      note: "Classic vertical, high contrast.",
      style: {
        backgroundColor: "#F5F5F0",
        backgroundImage:
          "repeating-linear-gradient(90deg,#2E7D52 0,#2E7D52 20px,#F5F5F0 20px,#F5F5F0 33px,#2E7D52 33px,#2E7D52 37px,#F5F5F0 37px,#F5F5F0 52px)",
      },
    },
    {
      name: "Harlequin",
      meta: "Dramatic + optic white · 64×48",
      note: "Diamond blocks, contrasting colour.",
      style: {
        backgroundColor: "#1A3A8F",
        backgroundImage:
          "linear-gradient(45deg,#F5F5F0 25%,transparent 25%,transparent 75%,#F5F5F0 75%),linear-gradient(45deg,#F5F5F0 25%,#1A3A8F 25%,#1A3A8F 75%,#F5F5F0 75%)",
        backgroundSize: "64px 48px",
        backgroundPosition: "0 0,32px 24px",
      },
    },
    {
      name: "Small Check",
      meta: "Everyday + optic white · 20px",
      note: "Wardrobe only — retired on screen.",
      style: {
        background: "repeating-conic-gradient(#3B1F2B 0 25%,#F5F5F0 0 50%) 0 0/20px 20px",
      },
    },
    {
      name: "Dots",
      meta: "Formal on Essence · 32px",
      note: "Closed shapes on an even field.",
      style: {
        backgroundColor: "#E8B98A",
        backgroundImage:
          "radial-gradient(#1B2A52 21%,transparent 22%),radial-gradient(#1B2A52 21%,transparent 22%)",
        backgroundSize: "32px 32px",
        backgroundPosition: "0 0,16px 16px",
      },
    },
  ].map((p) => (
    <div
      key={p.name}
      style={{
        border: "1.5px solid #1B2A52",
        borderRadius: "16px",
        overflow: "hidden",
        background: "#F5F5F0",
      }}
    >
      <div style={{ aspectRatio: "1", ...p.style }} />
      <div style={{ padding: "14px 16px", color: "#1B2A52" }}>
        <strong>{p.name}</strong>
        <div style={{ fontFamily: "monospace", fontSize: "11px", color: "#3B1F2B" }}>
          {p.meta}
        </div>
        <p style={{ fontSize: "13px", margin: "6px 0 0", color: "#3B1F2B" }}>{p.note}</p>
      </div>
    </div>
  ))}
</div>

Also on the sheet but not reproduced here as CSS (SVG-based): **Honeycomb**
(geometric hex cells, midcentury) and **Breeze Block** — "not on the
original list, but passes the rule: geometric, repeating, resolved.
The Palm Springs extension."

## Avoid

<div
  style={{
    border: "1.5px solid #1B2A52",
    borderRadius: "999px",
    padding: "16px 28px",
    display: "flex",
    flexWrap: "wrap",
    gap: "10px 14px",
    alignItems: "center",
    margin: "20px 0",
    background: "#F5F5F0",
  }}
>
  <strong
    style={{
      color: "#B71C2A",
      fontSize: "12px",
      textTransform: "uppercase",
      letterSpacing: "0.1em",
    }}
  >
    Avoid
  </strong>
  {[
    "Photorealistic prints",
    "Gradients",
    "Drop shadows",
    "Open-ended textures",
    "Two accents touching with no neutral between them",
  ].map((x) => (
    <span
      key={x}
      style={{
        fontSize: "13.5px",
        color: "#3B1F2B",
        textDecoration: "line-through",
        textDecorationColor: "#B71C2A",
      }}
    >
      {x}
    </span>
  ))}
</div>
