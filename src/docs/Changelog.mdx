import { Meta } from "@storybook/blocks";
import MarkdownIt from "markdown-it";
import changelogSource from "../../CHANGELOG.md?raw";

<Meta title="Docs/Changelog" />

export const md = new MarkdownIt();

Generated by [release-please](https://github.com/googleapis/release-please)
from commit history — imported live from `CHANGELOG.md` at build time,
so it can't drift.

<div dangerouslySetInnerHTML={{ __html: md.render(changelogSource) }} />
